<form method="post" action="{{ path('app_keyboard_delete', {'id': keyboard.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ keyboard.id) }}">
    <button class="btn">Delete</button>
</form>
